<h4>商品一覧</h4>

<button type="button" class="btn btn-primary rounded-circle p-0" style="width:2rem;height:2rem;">
  <%= link_to "＋", new_admin_item_path, class: "text-decoration-none" %>
</button>

<table>
  <thead>
    <tr>
      <th>商品ID</th>
      <th>商品名</th>
      <th>税抜価格</th>
    </tr>
  </thead>
  <tbody>
    <% @items.each_with_index do |item, i| %>
    <tr>
      <td><%= i + 1 %></td>
      <td><%= link_to item.name, admin_item_path(item.id) %></td>
      <td><%= item.price %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @items %>